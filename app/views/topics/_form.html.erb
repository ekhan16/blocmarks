<div class="row">
	<div class="col-md-8">
		<%= form_for @topic do |f| %>

			<div class="field">
				<%= f.label :title %><br>
			</div>
			<div class="actions">
				<%= f.submit %>
			</div>
		<% end %>
	</div>
</div>
